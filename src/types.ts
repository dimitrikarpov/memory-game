export type SecretFieldItem = { id: number; value: number }
export type SecretField = SecretFieldItem[]

export type GameFieldItem = { id: number; value: number | undefined }
export type GameField = GameFieldItem[]
